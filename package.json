{
  "name": "sitevision-proxy",
  "version": "1.0.0",
  "description": "A proxy for local sitevision assets.",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "redbird": "^0.3.5"
  }
}
